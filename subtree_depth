const ll mx=2e5+123; 
vector<ll>adj[mx]; 
bool vis[mx]; 
ll dept[mx]; 

void dfs(ll n)
{
    vis[n]=1; 
    dept[n]=0; 
    for(auto u:adj[n]){
        if(!vis[u])
        {
            dfs(u); 
            dept[n]=max(dept[n],dept[u]+1); 
        }
    }
}
