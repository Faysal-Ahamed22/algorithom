 
   while(l<=r){
    ll mid=(l+r)>>1; 
    if(arr[mid]>v) r=mid-1; 
    else   l=mid+1;

// inbuield function for finding x 
//binary_search(v.begin(),v.end(),x); if just need to know a element exist or not; 
// using set use find function if(s.find(x)!=s.end()) then element exist 
       
          
    
    
   
