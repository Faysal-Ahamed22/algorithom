const ll mx=2e5+123; 
vector<ll>adj[mx]; 
bool vis[mx]; 
vector<ll>sz(mx);
ll n; 
 
void dfs ( ll node )
{
    vis[node]=1;   
    for(auto u: adj[node])
    {
        if(!vis[u])
        {
           dfs(u); 
           sz[node]+=sz[u]; 
        }
    }
 
   
}
